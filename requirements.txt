# PyTorch CPU packages are installed separately so this project does not force
# CUDA or pull GPU dependencies.
#
# Windows + Python 3.11 CPU install:
#   python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Regular Python dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
librosa>=0.10
soundfile>=0.12
tqdm>=4.66
pyyaml>=6.0
